services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: driveintel-backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: driveintel-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend

